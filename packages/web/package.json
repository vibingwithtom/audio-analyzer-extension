{
  "name": "@audio-analyzer/web",
  "version": "1.0.0",
  "description": "Web-based audio analyzer application",
  "homepage": "https://audio-analyzer.tinytech.site",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:beta": "vite build --mode beta",
    "preview": "vite preview",
    "serve": "serve dist",
    "deploy": "npm run build && echo audio-analyzer.tinytech.site > dist/CNAME && gh-pages -d dist",
    "deploy:beta": "npm run build:beta && gh-pages -d dist -e beta"
  },
  "dependencies": {
    "@audio-analyzer/core": "^1.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "serve": "^14.2.0"
  },
  "keywords": ["web", "audio", "analyzer", "progressive-web-app"],
  "author": "vibingwithtom",
  "license": "MIT"
}